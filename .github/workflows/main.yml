name: Disable Workflow video

on: 
  push: 
      branches: [ main ]

jobs:
  disable:
     runs-on: ubuntu-latest
     steps:
       - name: Archit
         run: echo "Disable Workflow video DevOps"
